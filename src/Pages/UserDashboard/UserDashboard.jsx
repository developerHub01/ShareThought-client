import { Typography } from '@mui/material';
import React from 'react'

const UserDashboard = () => {
  return (
    <div>
      <Typography variant="h5">UserDashboard</Typography>
    </div>
  );
}

export default UserDashboard
