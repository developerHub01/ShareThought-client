import React from "react";

interface BlogLayoutProps {
  children: React.ReactNode;
}
const PostLayout = ({ children }: BlogLayoutProps) => {
  return <div>PostLayout</div>;
};

export default PostLayout;
